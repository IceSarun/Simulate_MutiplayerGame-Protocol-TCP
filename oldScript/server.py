import socket 

# ตั้งค่าพารามิเตอร์
# serverPort = 19000
# serverHOST = "192.168.1.4"

# def get_status_code(host, path="/"):
#     try:
# #         return conn.getresponse().status
# #     except SyntaxError:
# #         return None

# with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as servS:
#     servS.bind((serverHOST, serverPort))
#     servS.listen()
#     print("Ready connect")
#     conn, addr = servS.accept()

#     with conn:
#         print(f"Connected by {addr}")
#         while True:
#             data = conn.recv(1024)
#             print(data)
#             if not data:
#                 break
#             conn.sendall(data)





